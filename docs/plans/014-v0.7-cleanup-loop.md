# v0.7 cleanup loop

**Created:** 2026-05-10
**Branch:** `memex-v0.7`
**Purpose:** Get v0.7 to a tag-ready state via mechanical, non-architectural work. Designed to be executed as an iterative ralph-style loop (or by a fresh-context agent) with clear completion criteria.

## Requirements (decided 2026-05-10)

- **R1 — Audience:** memex is for the user across their own devices today (cross-device daemon model), eventually for other people too (memex is published). Implication: audit cleanup IS in scope; assume third-party readability when in doubt.
- **R2 — Offline behavior:** fail-closed. No local cache work. If it bites in practice, revisit.

These two requirements settle two of the four T2.x architectural questions. The remaining two (doc corpus location, daemon physical location) stay deferred — v0.7's current implementation is fine for now.

## Pre-decided judgment calls (so the loop doesn't stop)

- **AUD.M1 (homelab-named fixtures):** rename to generic placeholders (`host-a`/`host-b`, `Alex`/`Jordan` for personas, keep public model names like `Gemma`/`Qwen` as-is). Matches R1's "eventually shared" trajectory. *(deviating from this requires user input)*
- **AUD.M2 (pre-commit hook patterns):** move sensitive pattern list out of `.githooks/pre-commit` into a gitignored sidecar file the hook sources. Hook itself stays in repo and explains the indirection. Patterns themselves are the leak.

## Operating principle

Each item below is mechanically scoped: clear input, clear output, testable. **No item in this plan requires an architectural decision.** Decisions (T2.x in `013-post-v0.6.2-roadmap.md`) are explicitly out of scope.

The loop's job is to clean up; the user's job (separately) is to decide on architecture.

---

## In scope

Each item ends in one or more commits to `memex-v0.7`. Mark `[x]` when done; the loop reads this file to find the next unchecked item.

### CIT — Citation feature consistency
- [x] **CIT.1** Backport anchor format to `src/mcp-server.ts` `memory_recall` tool. (Returned full UUIDs; should return `[mem:abc12345 · category · scope]` matching the OpenClaw `tools.ts` shape.)
- [x] **CIT.2** Backport anchor-prefix support to `src/mcp-server.ts` `memory_forget` tool, using `expandAnchor` from `src/anchor.ts`. Return `anchor_ambiguous` / `anchor_not_found` errors consistent with the OpenClaw tool.
- [x] **CIT.3** Add tests for both MCP tool changes in `tests/mcp-server.test.ts` (or a new `tests/mcp-anchor.test.ts`).
- [x] **CIT.4** Update `plugin/memex-claude-code/README.md` to describe the citation-anchor behavior (was missing per earlier loose-end audit).

### AUD — Sensitive references audit (from `013` audit results)

**High severity:**
- [x] **AUD.H1** `plugin/memex-claude-code/README.md`: replace hardcoded Tailscale IP with `<your-memex-host>` placeholder.
- [x] **AUD.H2** `plugin/memex-claude-code/README.md`: replace `op://homelab/...` with `op://<your-vault>/...`.
- [x] **AUD.H3** `scripts/bakeoff` + `tests/latency-probe.ts`: genericize `op://dev-vm/dev-claude/...` to `op://<vault>/<item>/...`.
- [x] **AUD.H4** `docs/architecture.html`: same genericization.
- [x] **AUD.H5** `docs/research/embed-rerank-upgrade-brief.md`: replace explicit "Secrets go in 1Password under the `dev-claude` item in vault `dev-vm`." with placeholder.
- [x] **AUD.H6** `docs/research/embed-rerank-upgrade-brief.md` + `docs/design/model-bakeoff.md`: replace `~/homeinfra/...` paths with generic `~/<infra-repo>/...` or remove.
- [x] **AUD.H7** `docs/plans/2026-04-10-loop-report.md` + `CHANGELOG.md`: drop explicit `homeinfra` repo references in commit citations.
- [x] **AUD.H8** `docs/plans/012-memex-dreaming.md`: replace the hardcoded telemetry-relay worker subdomain with `<your-telemetry-relay>`.
- [x] **AUD.H9** `docs/design/mcp-server.md`: replace `100.x.x.x:8090` with `<embed-host>:<port>`.

**Medium severity (judgment call — fix or document as deferred):**
- [x] **AUD.M1** Decide on the homelab-named test fixtures (machine names like `host-X`, person names, model names) renames across ~20 files. Either rename consistently OR add a note to `.githooks/pre-commit` explaining the test-fixture exemption. (The audit report has the full list.)
- [x] **AUD.M2** Decide on `.githooks/pre-commit` pattern visibility. Either keep as-is (patterns are useful as a deny-list) or move patterns to a private gitignored config.
- [x] **AUD.M3** `docs/research/003-memory-retrieval-sota.md`: change example metadata strings from `homelab/infra` to `myproject/infra`.

**Post-fix:**
- [x] **AUD.X** Run `npm run build` after all source fixes so `dist/` is regenerated and committed without leaks.

### CHL — CHANGELOG hygiene
- [x] **CHL.1** Sync v0.6.0 / v0.6.1 / v0.6.2 entries from `main` into v0.7's CHANGELOG (currently v0.7's CHANGELOG only has the pre-reframe v0.6.0 entry).
- [x] **CHL.2** Add a `## [Unreleased]` or `## [0.7.0-dev]` section listing what v0.7 contains beyond v0.6.2: HTTP MCP daemon (per-session, bearer auth), dreaming + reflection, entity graph + temporal queries, Claude Code plugin with hooks, all the audit fixes from above.

### TYP — Type debt
- [x] **TYP.1** Run `npx tsc` — actually **172 errors** on v0.7 (more than main's 42 because v0.7 has more code). Breakdown:
  - **TS2341 × 75** — `Property 'db' is private and only accessible within class 'MemoryStore'`. Biggest single win: expose `db` (getter or public) on MemoryStore and ~75 errors disappear. Sites: cli.ts, dreaming.ts, mcp-server.ts, retriever.ts, etc.
  - **TS18047 × 44** — `Object is possibly null`. Needs `!` or null-check additions.
  - **TS2339 × 18** — `Property X does not exist`. Mix of better-sqlite3 type gaps (`Statement.pluck`, `Database.name`), `registerMemoryRuntime` missing on OpenClawPluginApi, etc.
  - **TS5097 × 12** — `Import path can only end with '.ts' extension when 'allowImportingTsExtensions' is enabled`. Test files importing `.ts` instead of `.js`. Fix: tsconfig flag OR update imports.
  - **TS2353 × 9** — `Object literal may only specify known properties` (schema drift like `LLMSessionOptions.timeout`).
  - **TS18048 × 3** — `Possibly undefined` (config.documents, context.embedder).
  - **TS2307 × 2** — `Cannot find module '@lancedb/lancedb'` (likely dead code in src/memory.ts + src/migrate-lancedb.ts).
  - **Misc × 9** — TS2349, TS2322, TS7017, TS7016, TS7006, TS2352, TS2345.
- [x] **TYP.2** Made `MemoryStore.db` public readonly. 75 TS2341 errors gone (172 → 97 total).
- [x] **TYP.3** No-op on v0.7 — `npx tsc 2>&1 | grep -i "stringenum\|typebox\|TUnsafe"` returns zero results. v0.7's typebox version (or call sites) doesn't trip the issue main had.
- [x] **TYP.4** Deleted dead `src/migrate-lancedb.ts` (zero callers); added `@ts-expect-error` on the optional dynamic import in `src/memory.ts` `loadLanceDB`. 97 → 95 errors.
- [x] **TYP.5** Fixed `timeout` → `maxDuration` in `src/doc-indexer.ts` (1 error). Remaining 8 TS2353 errors are in `tests/unified-retriever.test.ts` test fixtures passing `timestamp` to `Omit<MemoryEntry, ...>` — handled by TYP.6 batch.
- [x] **TYP.6** All remaining errors fixed across multiple batches: test imports `.ts`→`.js` (12), `store()` accepts optional timestamp for testability (8), null-narrowing on `store()` returns (44 across src/tools.ts + 5 test files), better-sqlite3 type gaps via `as any` (Statement.pluck, Database.transaction, Database.name, LlamaCpp.tokenize), `picomatch` @ts-expect-error (no types shipped), config.documents optional chains (3), embedder shape casts in test mocks (8), `globalThis.Bun` cast, MCP onsessioninitialized callback shape, registerMemoryRuntime cast, dreaming PluginConfig cast, fast-benchmark metadata cast, health-api memoryRuntime casts. **172 → 0 errors.**
- [x] **TYP.7** `package.json` `build` script changed from `tsc --noCheck` → `tsc`. Real type-check enforced; future type errors fail the build.

### REL — Release readiness (NOT tagging — that's user-driven)
- [x] **REL.1** Full test suite: 725/725 pass after better-sqlite3 native rebuild (Node 25.9 broke the prebuilt binding; ran `npm run build-release` in node_modules/better-sqlite3 to compile from source).
- [x] **REL.2** `npm run build` succeeds with real tsc (post-TYP.7). Output in dist/.
- [x] **REL.3** `clawhub package pack` succeeds: @ofan/memex@0.7.0-dev → 235806 bytes, SHA-256 eef4ce88...
- [x] **REL.4** `clawhub package install` does not exist (CLI offers `download` not `install` for packages). Used `clawhub package download @ofan/memex` instead — downloaded v0.6.2 successfully (1cc2dad6...); tarball contains expected `package/dist/src/*.js`. Live publish loop verified.
- [ ] **REL.5** Bump `package.json` + `openclaw.plugin.json` from `0.7.0-dev` → `0.7.0`. **Hand back to user — don't auto-bump and tag.**

---

## Out of scope

The loop **must not** touch these. They require user judgment or external resources.

- **All Tier 2 architectural decisions** (`013` T2.1-T2.4): doc corpus location, daemon location, offline behavior, correction semantics. The loop ships v0.7 with the *current* implementation of each (single daemon, no cache, fail-closed implicit, dreaming-style corrections).
- **Tag `v0.7.0`** and trigger the publish workflow. User decides when to ship.
- **MemoryAgentBench eval** (T4.2). External benchmark, costs $.
- **FadeMem decay, RL ops, multimodal, causal grounding** (Tier 5). New features, not cleanup.
- **Branch operations**: don't merge to main, don't force-push, don't delete tags.
- **CI / release workflow changes** beyond what's needed to make tests/build pass.

If an item the loop is working on *requires* an architectural decision to proceed, **stop and surface the question** rather than guessing.

---

## Loop operating rules

Each iteration:

1. **Read this file** to find the next unchecked `[ ]` item.
2. **Do that item.** One commit per item is preferred; one commit per logical group is acceptable.
3. **Verify** before claiming done:
   - `npm test` passes.
   - `npm run build` succeeds.
   - `git diff` looks like what was intended.
   - Commit succeeds (don't bypass `--no-verify`; if pre-commit hook blocks, fix the underlying issue).
4. **Mark `[x]`** in this file in the same commit (or a follow-up commit) so the next iteration knows what's done.
5. **Push** to `origin/memex-v0.7`.
6. **End the iteration.** Don't try to do multiple items per iteration unless they're tightly coupled.

If an item is unclear or genuinely blocked:
- Mark it `[?]` (not `[x]`) in this file with a one-line explanation.
- Surface the blocker in the iteration log below.
- Move on to the next item.

If all items are checked OR explicitly marked `[?]` with rationale, output:
```
<promise>V0.7 CLEANUP LOOP COMPLETE</promise>
```

**Hard cap: 25 iterations.** If not done by then, the remaining items get surfaced as user-decision items.

---

## Iteration log

Append after each iteration: what was done, what tests confirmed it, any unexpected discoveries.

<!-- (loop fills this in) -->

---

## Pointers

- Plan companion: [`013-post-v0.6.2-roadmap.md`](./013-post-v0.6.2-roadmap.md) — broader context, including out-of-scope work.
- Audit findings: in chat history (or re-run via the `general-purpose` agent against `desensitize audit` prompt).
- Citation feature reference impl: `src/anchor.ts`, `src/tools.ts` `memory_recall`/`memory_forget` (already on v0.7).
- v0.6.2 CHANGELOG and final state on `main`: source of truth for sync.
