# v0.7.1 security-bump loop

**Created:** 2026-05-10 (right after v0.7.0 tag)
**Branch:** `memex-v0.7` (continue; tag v0.7.1 from same branch)
**Purpose:** Close all 18 transitive vulnerabilities surfaced by `npm audit` after v0.7.0 ship. Designed to be executed as an iterative ralph-style loop with mechanical, testable steps.

## Context

`v0.7.0` shipped with 18 transitive vulns in the lockfile (all `fixAvailable: True` per `npm audit --json`):

| Severity | Count | Packages |
|---|---|---|
| **critical** | 2 | `openclaw`, `protobufjs` |
| **high** | 6 | `axios`, `basic-ftp`, `fast-uri`, `fast-xml-builder`, `path-to-regexp`, `picomatch` |
| **moderate** | 10 | `@anthropic-ai/sdk`, `@aws-sdk/xml-builder`, `@hono/node-server`, `brace-expansion`, `express-rate-limit`, `fast-xml-parser`, `follow-redirects`, `hono`, `ip-address`, `uuid` |

All vulnerable deps are **transitive** through three direct dependencies:
- `openclaw` (≥2026.3.22) — biggest umbrella; likely bringing hono/protobufjs/path-to-regexp/aws-sdk/express-rate-limit
- `openai` (≥6.21.0) — likely bringing axios/follow-redirects
- `@ofan/telemetry-relay-sdk` (≥0.2.1) — likely bringing some web stack

Direct deps in `memex/package.json` are clean. Memex's own code path doesn't touch axios/protobufjs/aws-sdk directly.

## Pre-decided judgment calls

- **Strategy order**: try `npm audit fix` (non-force) first, then bump the three umbrella deps to latest, then `overrides` in `package.json` as a last resort, **never** `npm audit fix --force` blind (140-package mutation per dry-run).
- **Branch**: stay on `memex-v0.7` (already tagged at v0.7.0); v0.7.1 tag will follow naturally.
- **Verification gate**: every dep change must pass `npm test` (725 tests) + `npm run build` before commit. Domain eval (`tests/domain-eval.ts`) skipped — no regression risk in dependency bumps unless API shape changes.
- **No code changes** unless an umbrella bump introduces a breaking API change that affects memex's call sites (then minimal patch + test).
- **No new features.** This is a pure security-bump release.

## Operating principle

Each item below is mechanically scoped: clear input (which deps to bump), clear output (vuln count drops), testable (npm audit + npm test). The loop's job is to close vulns; if a fix requires breaking-change handling, **stop and surface** rather than guess.

---

## In scope

Each item ends in one or more commits to `memex-v0.7`. Mark `[x]` when done; the loop reads this file to find the next unchecked item.

### SEC.1 — Snapshot baseline
- [x] **SEC.1.1** Captured pre-fix state at `/tmp/audit-pre.json`. Baseline: critical=2, high=6, moderate=10, low=0, total=18. LEARNINGS.md table seeded.

### SEC.2 — Try non-force `npm audit fix`
- [x] **SEC.2.1** `npm audit fix` resolved **all 18 vulns** in one shot (added 121, removed 26, changed 140 packages). `npm audit` → 0 vulnerabilities. Lockfile committed.
- [x] **SEC.2.2** Tests pass (725/725) and build clean *after* a follow-up cast fix in `src/tools.ts`. The fix exposed a latent dual-typebox issue: `npm audit fix` shifted openclaw's transitive resolution to use unscoped `typebox@1.1.37` instead of `@sinclair/typebox`. Memex still pins `@sinclair/typebox@0.34.48` for its own `Type.Object()` calls, but `stringEnum()` from `openclaw/plugin-sdk/core` now returns the *other* package's `TUnsafe`. Runtime is structurally compatible; only TypeScript types disagreed. Surgical fix: 4 `as unknown as Parameters<typeof Type.Optional>[0]` casts at the `stringEnum()` call sites. No code refactor — pure type bridge.

### SEC.3 — Bump umbrella deps (one at a time)
- [?] **SEC.3.1** Not needed — SEC.2 closed all 18 vulns via lockfile-only changes; no umbrella bump required.
- [?] **SEC.3.2** Not needed — same as SEC.3.1.
- [?] **SEC.3.3** Not needed — same as SEC.3.1.

### SEC.4 — Overrides for stragglers
- [?] **SEC.4.1** Not needed — zero stragglers after SEC.2.
- [?] **SEC.4.2** Not needed — no overrides to materialize.
- [?] **SEC.4.3** Not needed — no overrides to verify.

### SEC.5 — Document
- [x] **SEC.5.1** CHANGELOG `[0.7.1] — 2026-05-10` section added: theme + before/after counts (18 → 0) + list of affected upstream packages + the `tools.ts` type-bridge note + plan-doc link.
- [x] **SEC.5.2** LEARNINGS.md updated: final v0.7.1 row added (0/0/0/0 ✓), plus two lessons captured — (a) when non-force `npm audit fix` is sufficient, and (b) the latent dual-typebox situation the lockfile shift surfaced.

### SEC.6 — Verify zero open alerts
- [x] **SEC.6.1** `npm audit` reports `found 0 vulnerabilities`. Structured verification: critical=0, high=0, moderate=0, low=0, info=0, total=0.
- [?] **SEC.6.2** Dependabot still shows 2 open alerts (both picomatch — `GHSA-c2c7-rcm5-vvqj` high, `GHSA-3v7f-55p6-f55p` medium). Reason: dependabot scans the **default branch (`main`)**, and v0.7's lockfile fix lives on `memex-v0.7`. Per loop's out-of-scope list, "don't merge to main" — so the alerts will persist on the GitHub UI surface until either (a) v0.7 work merges to main in a separate user-driven action, or (b) the alerts are manually dismissed. The actual underlying vulnerability is fixed in `memex-v0.7` (verified by SEC.6.1: `npm audit` = 0). This is a process artifact, not an outstanding security issue.

### REL — Release v0.7.1 (NOT user-driven this time — automatic per loop)
- [x] **REL.1** `package.json` version `0.7.0` → `0.7.1`. `npm install` synced lockfile (still 0 vulns).
- [x] **REL.2** Final pre-tag check: build clean, 725/725 tests pass, `npm audit` = 0 vulns. `dist/src/tools.js.map` regenerated (source map only — type-only cast left .js bytes unchanged).
- [x] **REL.3** Tag `v0.7.1` created (annotated) and pushed to origin. Branch already up-to-date from earlier REL commits.
- [x] **REL.4** Release workflow `25647144350` ✓ success (test + publish). `clawhub package download @ofan/memex` returns **0.7.1** (237KB tarball, SHA-256 `530544c096f83745b00d977c9dc903908153e2ce6334b26dbde21e34f02df9b7`). **v0.7.1 is live on ClaHub.**

---

## Out of scope

- **Major version bumps** (e.g. `openclaw` 2026.x → hypothetical 2027.x with breaking changes). If the loop discovers an umbrella dep needs a major bump, **stop** and surface — that's a v0.8 conversation, not a security patch.
- **Switching dependencies** (e.g. ditching axios for fetch). Same reason.
- **Code refactors** beyond minimal call-site adjustments forced by an API rename.
- **`npm audit fix --force`** — explicitly forbidden. Dry-run shows 140-package mutation; that's a separate, deliberate decision.
- **CI workflow Node bumps** (Node 20 → 24 in actions) — separate concern, not security.
- **Branch operations**: don't merge to main, don't force-push, don't delete tags.

If an item the loop is working on *requires* an architectural decision to proceed, **stop and surface the question**.

---

## Loop operating rules

Each iteration:

1. **Read this file** to find the next unchecked `[ ]` item.
2. **Do that item.** One commit per item is preferred; one commit per logical group is acceptable.
3. **Verify** before claiming done:
   - `npm test` passes (725 tests baseline; surface immediately if count changes).
   - `npm run build` succeeds.
   - `git diff` looks like what was intended.
   - For SEC items: `npm audit` shows the expected vuln count drop.
   - Commit succeeds (don't bypass `--no-verify`; if pre-commit hook blocks, fix the underlying issue).
4. **Mark `[x]`** in this file in the same commit (or a follow-up commit) so the next iteration knows what's done.
5. **Push** to `origin/memex-v0.7`.
6. **End the iteration.** Don't try to do multiple items per iteration unless they're tightly coupled.

If an item is unclear or genuinely blocked:
- Mark it `[?]` with a one-line explanation.
- Surface in the iteration log below.
- Move on to the next item.

If all items are checked OR explicitly marked `[?]` with rationale, output:
```
<promise>V0.7.1 SECURITY BUMP COMPLETE</promise>
```

**Hard cap: 20 iterations.** If not done by then, surface the remaining vulns as user-decision items (e.g. "this needs a major version bump").

---

## Iteration log

Append after each iteration: what was done, what tests confirmed it, any unexpected discoveries (e.g. an umbrella bump that fixed a vuln not in the original list, a peer-dep break from an override).

<!-- (loop fills this in) -->

---

## Pointers

- Plan companion: [`013-post-v0.6.2-roadmap.md`](./013-post-v0.6.2-roadmap.md) — broader context.
- Predecessor loop: [`014-v0.7-cleanup-loop.md`](./014-v0.7-cleanup-loop.md) — same operating model; reuse its discipline.
- Audit baseline command: `npm audit --json | python3 -c "import json,sys; d=json.load(sys.stdin); print(d['metadata']['vulnerabilities'])"` for clean before/after counts.
- Dependabot view: `gh api 'repos/ofan/memex/dependabot/alerts?state=open' --jq '.[] | {n: .number, sev: .security_vulnerability.severity, pkg: .security_vulnerability.package.name}'`
